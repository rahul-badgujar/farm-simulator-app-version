import 'dart:ui';

const String appName = "Indian Farm Simulator";

/// UI Board Scale Level
enum UiBoardScale {
  Large,
  Medium,
  Small,
}

/// UI Board Type
enum UiBoardType {
  Stone,
  Parchment,
  Set,
}

/// UI Button Scale Level
enum UiButtonScale {
  Big,
  Medium,
}

/// Colors
const Color primaryColor = Color(0xFF795548);
const Color primaryAccentColor = Color(0xFF5D4037);
